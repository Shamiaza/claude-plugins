{
  "description": "Blocks direct file edits in main context - enforces delegation to agents",
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Edit|Write|MultiEdit|NotebookEdit",
        "hooks": [
          {
            "type": "command",
            "command": "echo '🚫 ORCHESTRATOR MODE: Direct file edits are blocked. Use Task tool with an agent (e.g., general-purpose, react-senior-dev) instead.' >&2 && exit 1",
            "timeout": 5
          }
        ]
      }
    ]
  }
}
